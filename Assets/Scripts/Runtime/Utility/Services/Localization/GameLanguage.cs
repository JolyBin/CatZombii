namespace Utility.Services.Localization
{
    /// <summary>
    /// Языки игры. Список закрытый и живёт в коде, а не в данных: ключи таблицы
    /// сериализуются полем на язык, и «добавить язык» — это правка структуры,
    /// которую обязан увидеть компилятор, а не тихая строка в ассете.
    ///
    /// Русский — ЯЗЫК-ИСТОЧНИК: на него падает фолбэк, и он один обязан быть заполнен
    /// целиком. Английский заведён пустым заделом — по docs/09 это фаза 2, переводить
    /// сейчас не надо, но структура обязана его держать, иначе перевод превратится
    /// в переделку.
    /// </summary>
    public enum GameLanguage
    {
        Russian = 0,
        English = 1,
    }
}
